# 會議紀錄：RepSafe 剩餘工作盤點——還要做什麼，還是差不多了？

- 日期：2026-09-24
- 輪數：2 輪
- 與會：Paula（PM）、Eddie（工程師）、Dana（設計師）、Sandy（業務）、Felix（財務）、Quinn（QA）
- 專案摘要：RepSafe 是 AI Builder Cup 2026 BFSI 賽道的作品（10/18 截止），賣家端反詐騙工具。已完成：後端骨架（90 passed、5 xfailed，BUG-001、002、004、005 已修）、手機單頁 UI（電商風格、範例對話、截圖預覽）、Cloud Run 離線模式上線（專案 `repsafe-2026`，預算 100 SGD）、各部門文件、GitHub。未完成：接真 Gemini 與 Web Risk、BUG-003 與其他中低風險 bug、真 Gemini 評測、真實案例、主辦回覆、3 分鐘影片。

## 一句話結論

**還沒差不多，大約六成。** 骨架、介面、部署和文件都有了，但沒有任何一項驗收門檻用真 Gemini 驗過，比賽要的「可運作原型」和 3 分鐘影片也還沒成立。關鍵路徑是：**9/28 切成真 Gemini → 33 則評測 → 10/9 功能凍結 → 10/10–10/16 拍影片**。10/5 排練時四條硬指標全部通過，才算「差不多了」。

## 共識

- **完成度約六成**，六人都同意。缺的是「用真模型驗證」和「影片」，不是再加功能。
- **不再做新 UI 功能**，Low 級 bug 全部不修；**10/9 功能凍結，之後只修 Critical**，日期不延。
- **修 bug 的順序**：BUG-003 排在接 Gemini 之前，讓 I02、I03 兩則 injection 不必完全靠模型擋。BUG-008 要提前修，否則冷啟動後第一則分析可能直接變灰卡。BUG-006、007 一起處理。
- **GCP 準備工作提前做**：啟用 aiplatform 和 webrisk API、給服務帳號加上 `roles/aiplatform.user` 和 `roles/webrisk.user`、確認 Flash 模型 ID。這幾件最容易卡住，不能留到最後一天。
- **SPF（新加坡警方）數字**：影片不放沒有來源的數字，格①改成純畫面，或用賣家的真實引言。
- **市場證據要從明天開始累積**：徵集案例時同時訪談，問損失金額和每月願付多少。10/5 前還沒有真實案例，就用標明「情境改寫」的匿名案例，不能假裝是真實案例。
- **工時餘裕只剩約 1 小時**，今天的會議也算。工時超過時先精簡影片，不動評測。

### 主持人更正（和現況對照）

- 這個專案**不用 API 金鑰**（用 ADC 和 Cloud Run 服務帳號），不需要 Secret Manager。Quinn 說的「打錯 API key」測試，改成「拿掉服務帳號權限」。
- 「偵測到 injection，卻把 scam_type 填成 none」是 **BUG-001，已經修好**。
- **失敗時判灰卡**，以及每小時請求上限（`GLOBAL_RATE_LIMIT_PER_HOUR`），程式裡已經有了，不必重做。灰卡「Can't determine」就是「無法驗證」的狀態，Dana 不用另外新增。
- 實際部署設定是**最多 1 台**，`docs/finance/budget.md` 寫的 2 台要改。
- **BUG-010**（測試裡的類真實網域）：老闆 9/24 已決定不換，不列待辦。
- **訪談逐字稿含個資**，只能放在 `data/private/`，不能進公開 repo。pitch 和數據文件沿用現有路徑 `docs/pitch/pitch-script.md`、`docs/sales/data-sources.md`。徵集主題是「假買家釣魚」，不是「被檢舉下架」。

## 仍有分歧

1. **預算警示門檻**
   - Felix：改成 50／80／90 SGD。花到 50 停掉非必要呼叫，花到 80 評測縮成 20 則且只跑 1 次，花到 90 關掉 Cloud Run、只留影片。
   - 現況：GCP 裡設的是 50％／90％／100％。要不要改，待老闆決定。
2. **評測門檻**
   - PRD 和 test-plan：20 則詐騙最多漏報 2、10 則正常最多誤判 1、injection 3/3 全擋。
   - Quinn 這次提的：誤判最多 2，另外加上「兩次結果不一致最多 3 則」。
   - 主持人建議以 PRD 為準，一致性當作附加條件。待老闆決定。
3. **切換成真 Gemini 的日期**
   - Paula：9/29 前。
   - Eddie：9/28 切換，9/29 留給 Quinn 測冷啟動。
   - 兩者沒有衝突，採 Eddie 的較早版本。

## 各角色待辦

| 角色 | 待辦 |
|---|---|
| Paula（PM） | ① 把本次凍結規則和最終時程寫進 `docs/roadmap.md` ② 每天追工時回報 ③ 10/5 主持排練、判定四條硬指標 |
| Eddie（工程師） | ① 9/25 修 BUG-003（`app/`、`tests/test_qa_adversarial.py`）② 9/26 做 GCP 準備工作（API、IAM、模型 ID），9/27 修 BUG-008、006、007 ③ 9/28 照 `docs/engineering/deploy.md` 切成真 Gemini，並告訴 Felix 每次呼叫的 token 數 |
| Dana（設計師） | ① 9/28 截三則真輸出（詐騙、正常、灰卡）② 9/30 前交文案問題清單（只改字），`docs/design/storyboard.md` 格①改成不放數字 ③ 10/5 真機排練 0:28–0:42 這段 |
| Sandy（業務） | ① 9/25–9/29 徵集案例並訪談（目標 5 位、最少 3 位），逐字稿放 `data/private/` ② 9/30 更新 `docs/sales/data-sources.md` ③ 10/2–10/3 把定價區間和 FAQ 寫進 `docs/pitch/pitch-script.md` |
| Felix（財務） | ① 9/25 記下目前的帳單花費 ② 把 `docs/finance/budget.md` 的最多台數改成 1，並依老闆決定更新停損線 ③ 10/1 前從評測紀錄算出單次分析成本下限，交給 Sandy |
| Quinn（QA） | ① 9/29 冷啟動實測（無痕視窗連開 10 次）② 9/30–10/4 跑 33 則評測，每則 2 次，加 5 則 injection 回歸 ③ 實測觸發上限時的畫面、拿掉權限時的降級，更新 `docs/qa/test-plan.md` |

## 錢的問題

- **預估成本**：
  - 單次分析成本：待估算（10/1 前由 Felix 從評測紀錄算出）
  - 評測總成本（33 則 × 2 次，加截圖）：待估算
  - 目前已經花掉的金額：待估算（9/25 查帳單）
- **需要資金**：預算上限 100 SGD，已設警示。停損線要不要改成 50／80／90，待老闆決定。
- **回本時間**：定價區間要等成本下限和訪談問到的願付金額；回本時間待估算。
- **工時**：25 小時上限，已用多少待估算；餘裕約 1 小時。

## 主要風險

1. **9/28 接不上真 Gemini**，原因可能是權限、模型 ID 或輸出格式。評測和影片都卡在這一步。
2. **injection 門檻 3/3 沒過**：BUG-003 還沒修之前，I02、I03 只能靠 Gemini 擋。
3. **冷啟動**：評審第一次打開可能等很久或看到灰卡（BUG-008）。
4. **工時超過上限**：餘裕只剩約 1 小時。
5. **沒有市場證據**：拿不到真實案例和「願意付費」的原話，pitch 就站不住。
6. **主辦不允許交兩件**：整件作品會觸發停損。

## 建議老闆下一步

1. **9/30 前寄主辦詢問信**（草稿在 `docs/qa/organizer-inquiry-draft.md`），順便問能不能附使用者證據。
2. **明天開始找賣家**，9/29 晚上盤點。
3. **決定兩個門檻**：預算停損線（維持 50/90/100%，還是改成 50/80/90 SGD），以及評測的誤判上限（1 還是 2）。

## 附錄：完整發言紀錄

### 第 1 輪

【Paula（PM）】我的判斷是：工程骨架差不多完成了，但照比賽和 PRD 的標準來看，大概只到六成。現在線上跑的是離線模式，test-plan.md 第 5 節的驗收門檻還沒用真 Gemini 驗過任何一項。I02、I03 兩則 injection 因為 BUG-003 程式擋不下來，只能靠 Gemini；照 test-plan.md 的現況表，離線模式下 injection 只擋住 1/3，門檻要求 3/3 全擋。所以「可運作原型」這一項，現在嚴格說還不能算數。關鍵路徑我排成這樣，前一步沒完成，後一步就沒辦法開始：一、10/5 前接上真 Gemini 和 Web Risk，同一週把 BUG-003 修好。這條最優先，其他工作都卡在它後面。BUG-006、007 能順手修就修，排不進來就先放。二、跑 33 則評測，對照 PRD 第 9 節的門檻，也就是漏報不超過 2、誤判不超過 1、injection 全擋、熱機 P90 在 10 秒內。三、10/9 功能凍結。沒過門檻就把功能範圍縮小，凍結日不延後，這是 roadmap.md 寫死的。四、10/10 到 10/16 拍 3 分鐘影片。腳本直接用 pitch 文件，問題陳述和社會效益兩段現在就可以先寫。五、真實案例徵集和主辦回覆可以同時跑，但不能卡住主線。主辦的五個問題 9/30 前一定要寄出去，Quinn 的草稿已經寫好了。我主張現在就砍掉的：新 UI 功能、LineSleuth 的時間，還有 Low 級 bug。真實案例如果 10/5 前還收不到，影片就改用「匿名改寫的典型案例」，不要為了這件事等。Eddie，我想先確認一件事：接 Gemini 要花幾天？要是超過 5 天，評測的時間會很吃緊。

【Eddie（工程師）】Paula 問接 Gemini 要幾天，我的答案是 3 個工作天，9/29 前可以上線，不會超過 5 天。後端已經留好離線/線上切換，要做的是：第 1 天接 Gemini 結構化輸出，把回傳格式對上現有 schema；第 2 天接 Web Risk，順便加逾時和降級，查不到就退回離線判斷；第 3 天在 Cloud Run 設 Secret Manager 放金鑰、重新部署，照 docs/engineering/deploy.md 的流程走。BUG-003 其實只是關鍵字比對沒涵蓋「Ignore your previous instructions」和「不要理會前面的規則」這類變體，半天就能修好，3 個 xfail 應該能拿掉。BUG-006、007 我也一起處理。不過我要補一個風險：docs/qa/bugs.md 寫到，injection 能不能 3/3 全擋，最後還是看 Gemini 會不會「偵測到了，scam_type 卻填 none」。所以我建議程式端加一道保險，只要偵測旗標是 true，就強制升到紅燈，不讓模型自己說了算。這樣門檻就不用賭模型的表現。另外，33 則評測要跑好幾輪，API 費用要請財務幫忙盯。100 SGD 夠用，但我會加上每日上限。我同意 Paula 砍掉新 UI 功能，10/9 凍結也沒問題。

【Dana（設計師）】我同意 Paula 和 Eddie 砍掉新 UI 功能，也贊成 10/9 凍結。不過設計這邊的工作還沒結束，重點會從「做」變成「驗」。第一，storyboard.md 第 ③ 格的紅卡 chip 寫著「以實際輸出為準」，第 ⑤ 格的琥珀卡和 injection 被擋的畫面，目前也都只有離線模式的樣子。Eddie 9/29 接上真 Gemini 之後，文案長度和 chip 名稱可能會變，卡片會不會擠爆、會不會換行跑版，我要在真機上重新走一遍。所以我請 Eddie 上線當天丟三則真實輸出給我，我 9/30 前回報要調哪些字。第二，時間卡得最緊的是 0:28–0:42 這 14 秒：上傳截圖、確認文字、按下檢查全擠在這裡。我建議 10/5 評測跑完就先拿真機排練一次計時，不要等到 10/10 才發現超時。超時的話照文件寫的，先砍「收合」那個動作。第三，第 ① 格的 SPF 數字還標著「待驗證」。Sandy 要是 10/5 前還找不到可信來源，就直接改成純畫面停 3 秒，不要硬放數字。UX 雖然只占 10%，但影片是評審對這個產品的第一印象，技術分那 40% 也是透過畫面被看見的。

【Sandy（業務）】我同意 Paula 說的「約六成」，不過從業務角度看，缺的不只技術驗證，還少了一句評審會記住的「誰會付錢」。現在 docs/pitch/pitch-script.md 講的是功能，我手上沒有任何一個賣家親口說過「我願意用」。我的提案是把 9/25–9/29 的真實案例徵集和賣家訪談合在一起做。照 docs/sales/seller-outreach.md 的名單，每聯絡一位賣家，除了要案例，也順便問兩題：「上次被騙損失多少？」「每月願意付多少錢防詐？」拿到 3 句真實引言，影片第一格就能換成賣家原話，比 Dana 說的 SPF 數字更有說服力，也不用擔心來源可不可信。Paula 的停損點我支持：9/29 案例不到 3 則就改用匿名改寫版。但訪談引言照樣要收，哪怕只有 1 句也放進影片。另外，Eddie 設的每日費用上限，請財務順便換算成「每次檢查成本」，我好在簡報上寫出定價區間。技術可以晚點收尾，市場證據卻得從明天就開始累積。

【Felix（財務）】我同意 Paula 的判斷，還差得遠。從錢的角度看，現在最大的漏洞是 docs/finance/budget.md 裡「單次分析成本」那一格還是空的。沒有這個數字，Eddie 要的每日上限和 Sandy 要的定價區間都只能用猜的。回應 Sandy：每次檢查的成本目前是「待估算」，我不會先報一個數字給你寫進定價。Eddie 9/29 接上 Gemini 後，33 則評測跑第一輪，我從 log 讀 token 數，取平均和最大值各算一次，截圖的呼叫另外算，10/1 前給你成本下限。定價的另一半，也就是賣家每月願意付多少，就靠你 9/25 到 9/29 問到的數字，兩邊都有了才能寫區間。回應 Eddie：GLOBAL_RATE_LIMIT_PER_HOUR 評測期間維持 120，10/12 後調到 30。另外有個出入：摘要寫 max-instances 是 1，budget.md 寫的是 2，最壞情況要用哪個數字算，請確認實際設定。還有一個風險：工時餘裕只剩 1 小時，今天這場會議也要記進工時。9/27 對帳時如果雜項已經超過 1 小時，影片就直接走精簡版。

【Quinn（QA）】我同意 Paula 說的，現在大概六成。不過我要補一個風險：docs/qa/bugs.md 裡標成 High 的那條，是 Gemini「有偵測到 injection，但 scam_type 填了 none」。I02、I03 現在只能靠 Gemini 擋，這種情況只要出現一次，3/3 的門檻就不過了。所以 Eddie 修 BUG-003 那半天要排在接 Gemini 之前，至少先把 I02、I03 救回來，不要全押在模型上。另外 BUG-008 我建議提前處理，不要等到部署當天。Cloud Run 冷啟動後，/health 暖不到 Gemini 和 Web Risk 的 client，評審第一次打開很可能就看到灰卡。9/29 上線那天我會實測冷啟動，量第一則分析要幾秒。Dana 的 10/5 真機排練，我會帶 E08 和全形網址（BUG-006）一起測。Felix，33 則評測我建議每則跑 2 次，看結果穩不穩定，額度要算兩倍。BUG-010 的網域要在公開 repo 前換成保留網域，最晚 10/9 換完。

### 第 2 輪

【Paula（PM）】我的結論是完成度大約六成。骨架都有了，但沒有任何一項門檻用真 Gemini 驗過，所以還不能說「差不多了」。先依主持人的更正調整：不用 API 金鑰、不用 Secret Manager；BUG-001 已經修好；BUG-010 照老闆決定不列入。最終待辦如下：Eddie 9/25 修 BUG-003，9/26 一起修 BUG-008、BUG-006、BUG-007，9/29 前接好 Gemini 和 Web Risk 並上線，每日上限照 Felix 的數字，評測期每小時 120、10/12 後改 30；Eddie 9/25 把 budget.md 的 max-instances 改成 1。Quinn 9/29 實測冷啟動，9/30 到 10/4 把 33 則評測每則跑 2 次，10/5 排練要帶 E08 和全形網址。Dana 上線當天給三則真實輸出，9/30 前回報文案；10/5 真機排練；SPF 數字沒有來源就改成純畫面。Sandy 9/25 到 9/29 徵集案例，同時問損失金額和每月願付多少，10/5 前沒拿到真實案例就改用匿名改寫。Felix 10/1 前從評測 log 算出單次分析成本的下限，9/27 對一次工時。老闆 9/30 前寄出主辦信。影片 10/10 到 10/16 製作，10/17 留作緩衝。凍結規則：一、10/9 凍結程式碼，之後只准修 Critical，不延期。二、不做新 UI，Low bug 全部砍掉。三、9/27 對帳時工時超過，影片直接精簡，不加班擠時間。四、9/29 真 Gemini 沒上線，評測就縮到 20 則，凍結日期不動。

【Eddie（工程師）】Paula 的日期我接。不過有三個前置條件要先講清楚。第一，9/25 修 BUG-003 的同時，我會把 budget.md 的 max-instances 改成 1。這兩件都小，當天就能做完。第二，deploy.md 寫的切換步驟，我建議提前到 9/26 下午先跑前半段：啟用 aiplatform 和 webrisk 兩個 API，替服務帳號加 roles/aiplatform.user 和 roles/webrisk.user，再在 us-central1 或 asia-east1 用一次 curl 確認 Flash 的模型 ID 能呼叫。權限跟模型 ID 是最常卡住的地方，留到 9/29 才發現就來不及了。等 9/27 把 BUG-008、006、007 修完，9/28 再 update-env-vars 切到真 Gemini，9/29 只留給 Quinn 測冷啟動和修補。第三，Felix 說工時只剩 1 小時，所以每小時上限我只做環境變數加一個計數器。max-instances 是 1，記憶體計數就夠準，不用接 Redis。Web Risk 有配額，評測 33 則×2 不會爆，但我會先接好 fallback：真呼叫失敗時回傳「無法驗證」，不能當成安全。凍結規則我同意，10/9 以後 main 只收 Critical 的修正。

【Dana（設計師）】我接 Eddie 的新時程，把 Paula 清單裡我的部分收斂成四個日期：一、9/26：先把「無法驗證」狀態的文案和樣式定好。Eddie 說 Web Risk 失敗不當安全，那畫面上就不能出現綠色，我改用灰底，文案寫「暫時無法確認，請自行小心」。這個不算新 UI，是補一個狀態，要趕在 10/9 凍結前做完。二、9/28：Eddie 切真 Gemini 那天，我同一天截三則真輸出：詐騙、正常、無法驗證各一則，檢查語氣、長度，還有手機上會不會斷行。三、9/30 前：把文案問題清單交給 Eddie。只改字、不改版面，方便他 10/1 前一次改完。SPF 那段確定拿掉數字，改成純畫面。四、10/5：跟 Quinn 一起用真機排練，重點盯 0:28–0:42 這段。補充一點：如果 9/29 還沒上線，我 9/30 就用 mock 輸出先定稿，不等。影片 10/10 開拍時畫面不會再動。

【Sandy（業務）】我接 Paula 排的 9/25–9/29 這段，業務這邊的日期和交付物如下：9/25 在賣家社團和 LINE 群發徵集文，拿「被檢舉下架」的真實案例。每個案例都要問三件事：損失多少錢、多少天沒出貨、每個月願意付多少錢避免這種事。9/26–9/29 目標訪談 5 位賣家，最少 3 位，每位 15 分鐘，逐字稿存 sales/interviews/。9/30 把訪談結果整理成數據來源表。每個數字都標出處，沒有來源的數字一律不放進 pitch。10/2 Felix 的成本下限 10/1 出來，我隔天就把定價區間寫進 sales/pitch.md。10/3 完成 sales/pitch.md 和 sales/faq.md 初稿，給 Dana 對語氣。10/5 排練時跑一次 pitch，由 Quinn 挑毛病。同意 Dana 拿掉 SPF 數字。我們沒有來源的數字，評審一問就露餡。影片開場要用真人引言，所以 10/5 還沒拿到真實案例的話，就照 Paula 說的匿名改寫，並且標注「情境改寫」，不要假裝是真實案例。老闆寄主辦信的時候，順便問一下能不能附使用者證據。

【Felix（財務）】我接 Paula 的凍結規則，把財務這邊的停損數字講清楚。9/25 我會先查 GCP 帳單，把目前已經花掉的錢記下來，現在這個數字還是「待估算」。預算停損線這樣訂：100 SGD 花到 50 就停掉所有非必要的呼叫；花到 80 評測縮成 20 則，而且只跑 1 次；花到 90 就關掉 Cloud Run，只保留錄好的 demo 影片。Quinn 要每則跑 2 次，額度就算兩倍，所以 Eddie 9/28 切換真 Gemini 之前，要先告訴我每次呼叫用多少 token，我才能算評測總成本，這個數字目前待估算。9/27 我會和 Eddie 一起對帳，確認 budget.md 裡的 max-instances 已經改成 1，而且預算警示有設在 50、80、90。10/1 我會把單次請求的成本下限交給 Sandy，她 10/2 寫定價區間要用。訪談問到的每月願付金額只放匿名的彙總數字，逐字稿放 data/private/。工時方面，25 小時裡已經用掉多少還是待估算，請大家每天回報一次。剩最後 1 小時餘裕的時候，先依照 Paula 說的精簡影片，不要動評測。

【Quinn（QA）】我同意 Paula 的排程，但「差不多了」要看硬指標，不能只看日期。我提四條驗收判準：第一，冷啟動。9/29 在 Eddie 切成真 Gemini 之後，無痕視窗連打 10 次，首次回應 p95 要在 15 秒內，而且 0 次白屏。逾時一定要落到灰卡，不能卡在 loading。第二，評測。33 則跑兩次，詐騙類召回率至少 90%，正常訊息誤判成高風險最多 2 則，兩次結果不一致的不能超過 3 則。BUG-001 雖然修了，我會加 5 則注入樣本做回歸，旗標和 scam_type 必須一致。第三，停損。Felix 定了 50、80、90 SGD 三道線，我要實測觸發每小時上限時使用者看到的是什麼。另外要故意打錯 API key，確認有降級，不會直接噴錯。第四，Dana 截的三則真輸出，要和影片、pitch 裡的數字逐字對過，Sandy 的數據來源表也要對上。10/5 排練時四條全綠才算差不多。只要有一條紅，就照 Paula 的凍結規則縮範圍，不硬上。
